name: 🦾 Cập nhật hoạt động gần đây

on:
  schedule:
    - cron: '0 * * * *' # cập nhật mỗi giờ
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: Readme-Workflows/recent-activity@v2.0.0
        with:
          GH_USERNAME: PhungVietBac
        env:
          GITHUB_TOKEN: ${{ secrets.MY_SECRET_TOKEN }}
      - run: echo "Workflow completed successfully!"
